---
import HomePage from "../components/pages/homePage/HomePage.tsx";
import Layout from "../layouts/Layout.astro";
import { generateMetaTags } from "../components/generateMetaTags";
const currentPage = "Home";
const siteName = "Arky's Portfolio";
const title = `${currentPage} | ${siteName}`;

const description =
  "Hi I'm Arky, an experienced developer that provides technical, impactful & collaborative solutions to an application’s frontend, backend and cloud infrastructure";
const url = Astro.url;
const profileImg = `${process.env.REACT_APP_MEDIA_FILES_URL}/appLogo.jpg`;
const seo = generateMetaTags({
  title,
  siteName,
  description,
  site: url.toString(),
  image: profileImg,
  imageAlt: "Profile Photo of Arky Asmal",
  determiner: currentPage,
});
// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout seo={seo}>
  <HomePage client:only="react" />
</Layout>
